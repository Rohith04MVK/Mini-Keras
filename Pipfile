[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
numpy = "*"
scipy = "*"
matplotlib = "*"

[dev-packages]

[scripts]
lint = "pre-commit run --all-files"
precommit = "pre-commit install"
